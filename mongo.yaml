version: '3'
services:
  mongodb:
    image: mongo
    container_name: mongodb
    ports:
      - "27017:27017"
    volumes:
      - ~/data/mongodb:/data/db
    environment:
      MONGO_INITDB_ROOT_USERNAME: HetuCrelay
      MONGO_INITDB_ROOT_PASSWORD: hetcrelay666